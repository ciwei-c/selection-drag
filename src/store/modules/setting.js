import variables from '@/styles/color.scss';

const state = {
  theme: variables.primary,
  colorVars: variables
};
const mutations = {
};

const actions = {};

export default {
  namespaced: true,
  state,
  mutations,
  actions
};
